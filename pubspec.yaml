name: filesize_plus
description: A package to get humanized file sizes. Translate raw file size into human readable strings. With null safety support!
homepage: https://github.com/aguilaair/filesize

version: 1.1.1+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dev_dependencies:
  test: ^1.16.5
  test_coverage: ^0.5.0
  pedantic: ^1.4.0
